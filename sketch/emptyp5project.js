function setup() {
	let canvas = createCanvas(
		window.innerWidth /2,
		window.innerHeight /2
		);

	// canvas.parent('sketch-holder');

}

function draw() {
	background(200);
	fill(100); 
	rect(width/2, height/2, 50, 50);
	//  
}